server.port=8082

spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:postgresql://localhost:5433/user_db}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:postgres}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:nouveaumdp}
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

spring.security.oauth2.resourceserver.jwt.jwk-set-uri=${SPRING_SECURITY_OAUTH2_RESOURCESERVER_JWT_JWK_SET_URI:http://localhost:8080/realms/ebanking-realm/protocol/openid-connect/certs}

spring.graphql.graphiql.enabled=true

# Main SMTP Configuration
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=saida.hourani12@gmail.com
spring.mail.password=vkbx rabw grls vnrm
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# Keycloak Admin Configuration
keycloak.admin.server-url=http://keycloak:8080
keycloak.admin.realm=master
keycloak.admin.username=admin
keycloak.admin.password=admin
keycloak.target.realm=ebanking-realm
keycloak.target.client-id=admin-cli

logging.level.org.springframework.security=DEBUG
logging.level.org.springframework.security.oauth2=DEBUG
